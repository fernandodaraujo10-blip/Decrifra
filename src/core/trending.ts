
import { UnifiedSearchResult } from './types';

export async function getTrending(): Promise<UnifiedSearchResult[]> {
    // Implementação simplificada de populares (poderia vir de um endpoint específico do TMDB)
    // Por enquanto, retornamos vazio para ser preenchido se houver uma API Key válida
    return [];
}
